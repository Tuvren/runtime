# Core Types Reviewed Artifacts

Epic R activates this directory as the reviewed-artifact home for
machine-readable shared core-type outputs.

- Files here are reviewed downstream artifacts.
- They must not expand `shared/` into a semantic authority shortcut.
