# Tool Contracts Root

Language-neutral tool-contract assets live at this contract root:

- `spec/` for the reviewed TypeSpec source
- `artifacts/` for the reviewed JSON Schema and OpenAPI outputs

The current TypeScript package implementation for `@tuvren/tool-contracts`
lives under `implementations/typescript/`.
