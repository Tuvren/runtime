# Provider API Contract Root

Language-neutral provider-contract assets live at this contract root:

- `spec/` for the reviewed TypeSpec source
- `artifacts/` for the reviewed JSON Schema and OpenAPI outputs

The current TypeScript package implementation for `@tuvren/provider-api` lives
under `implementations/typescript/`.
