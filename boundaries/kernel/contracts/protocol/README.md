# Kernel Protocol Contract Root

Language-neutral kernel protocol assets live at this contract root:

- `spec/` for the reviewed CDDL grammar
- `artifacts/` for neutral companion outputs

The current TypeScript package implementation for `@tuvren/kernel-protocol`
lives under `implementations/typescript/`.
