{
  "name": "kernel-typescript-conformance-adapter",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "root": "boundaries/kernel/implementations/typescript/conformance-adapter",
  "sourceRoot": "boundaries/kernel/implementations/typescript/conformance-adapter/src",
  "projectType": "library",
  "tags": [
    "boundary:kernel",
    "language:typescript",
    "layer:conformance-adapter"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "boundaries/kernel/implementations/typescript/conformance-adapter",
        "command": "bunx --bun tsup --config tsup.config.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "bunx --bun @biomejs/biome check boundaries/kernel/implementations/typescript/conformance-adapter"
      }
    }
  }
}
