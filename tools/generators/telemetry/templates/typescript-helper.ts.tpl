/**
 * Repo-owned TypeScript telemetry helper template path.
 *
 * Epic R still renders the single TypeScript consumer directly in
 * `tools/scripts/telemetry-codegen.ts`, but this checked-in template keeps the
 * language-agnostic generation plan explicit and reviewable.
 */
