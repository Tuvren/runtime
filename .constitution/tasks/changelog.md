# Stage 4 (Tasks) Changelog

Local SemVer history for `.constitution/tasks/`.

- v0.38.0 - Closed **Epic BH — Conversation-State Ownership Hardening (KRT)**, the fourth (and lower-end-sized) epic of the SaaS-Readiness block, realizing ADR-053 (Tuvren is the unconditional conversation-state owner; provider server-side state, carried continuity artifacts, and provider-side caching are reconstructable optimizations, never a correctness dependency) and delivering the ADR-055 baseline-bridge `providerExecuted`/`dynamic` fidelity audit, for PRD CAP-P0-069 exactly as accepted — no ADR text changed. All five tickets landed and each milestone was independently reviewed by an isolated `reviewing-milestone-commits` subagent over its commit SHA, with valid P0/P1/structural findings (and one P2 worth pinning) fixed via a review-follow-up commit before the next milestone. **KRT-BH001** aligned machine authority first (declare-then-promote, the BE/BF/BG precedent): added the `provider-api-conversation-state.json` plan with checks gated behind a new `providers.conversation-state-ownership` capability bit and registered it in the provider-api authority packet, keeping the §1.1 coverage-matrix entry honestly `missing-conformance-follow-up` until closeout — no runtime behavior. **KRT-BH002** proved a next-turn provider request is reconstructable purely from rebuilt durable head-state with no provider-held state (reconstructed request equals the live-path request) and proved continuity artifacts are shreddable; the open question resolved to *no new `provider.continuity` edge* because message-part continuity (`providerMetadata` on assistant/reasoning/tool parts) is already crypto-shreddable through the BF005 `MESSAGE_PAYLOAD_EDGE` codec (typed `ErasedPayload` on key destruction, byte-identical lineage hashes), so M2 asserted that existing coverage rather than adding a persistence surface. **KRT-BH003** closed the AY005 multi-turn `providerContinuity` round-trip gap (recorded in tech-spec v0.29.3) with a real multi-turn test; the existing message-part `providerMetadata` replay already round-trips turn-1 continuity into the turn-2 prompt, so the test is the deliverable and no driver extraction code was added — a docs follow-up clarified the two-artifact layering boundary (per-message-part replay vs. turn-level `TuvrenPrompt.providerContinuity`) so the test's scope is not mistaken for turn-level extraction. **KRT-BH004** proved provider-side caching is correctness-neutral — a cache miss and a cache hit for the same turn produce an identical canonical result, only cost/latency differ — and the review follow-up scoped the proof to the *produced content* (the model-facing outcome) and exercised the real cost path; the bridge's `providerMetadata.aiSdkBridge.rawUsage` cost bookkeeping (whose `cacheRead` varies with the cache) is persisted into the content-addressed durable record, a pre-existing cost-bearing-not-correctness-bearing coupling recorded as a deferred follow-up observation in `tech-spec/changelog.md` v0.30.4. **KRT-BH005** performed the ADR-055 baseline-bridge `providerExecuted`/`dynamic` fidelity audit against vercel/ai #10888 (open as of 2026-06): the bridge is structurally immune (it uses the low-level `LanguageModelV3.doGenerate`/`doStream` contract, never `parseToolCall`, and routes provider-executed tools by the host's *declared* tools, not the user function tool map), but the audit found and fixed a real defect — `rejectUnsupportedProviderOwnedToolPart` rejected *every* `providerExecuted`/`dynamic` `tool-call` regardless of declaration, so a realistic provider-executed round-trip (inline `tool-call` before its `tool-result`, verified against `@ai-sdk/openai@3.0.53`) aborted before reaching the `tool-result` carrying provider-native attribution; the fix skips a *declared* provider-executed call (its `tool-result` carries the AY002/AY004 provider-native attribution) and still rejects an *undeclared* one, symmetrically across the generate and stream paths. Per-class observation limits (no cancel/retry/audit) already hold via the existing KRT-AY003 runtime test (no runtime change). The M5 review surfaced one P2 — an orphan *declared* provider-executed call (whose `tool-result` never arrives) was silently skipped where the prior over-broad path threw — which the review-follow-up commit pinned with generate + stream tests (orphan call yields no observation and no throw; the result is the attributable observation, the call is provider bookkeeping) plus a fuller stream `owner == "provider"` assertion, deliberately adding no orphan-tracking state (deferred to the ADR-055 native-client phases). The audit is recorded in `.constitution/reports/bh005-bridge-provider-executed-fidelity-audit.md`; `ai@6.0.142` / `@ai-sdk/provider@3.0.8` confirmed locked. **Closeout promotion:** the framework spec §1.1 v0.21 conversation-state-ownership note flipped from `missing-conformance-follow-up` to `authority-backed-conformance-covered` via a dedicated `conversationStateOwnership` evidence anchor in the docs-authority freeze gate (adapterCapability `providers.conversation-state-ownership`, provider-api authority packet, the runnable `provider-api-conversation-state.json` plan, provider fixture, providers compatibility evidence — all five resolve on disk); the frozen §1.1 spec prose was untouched (versioned mechanism language; BG already promoted the other two SaaS-readiness target semantics without editing it), and per-lane compatibility evidence was refreshed (freeze gate `--check`: 264 classified claims, `authority-backed-conformance-covered` 223 → 224, `missing-conformance-follow-up` 5 → 4). Close: archived the epic via `git mv` into `.constitution/tasks/completed/`; recomputed active story points 37 → **21 remaining** (BI 21 only) and marked Epic BH CLOSED across the executive summary, critical path, active scope, build-order flowchart, and definition of done; recorded the realization in `tech-spec/changelog.md` v0.30.4 and added Epic BH to the completed-work ledger. Validation: `bun run check`, `bun run verify:kernel` (memory/SQLite/PostgreSQL conformance), and `bun run verify` (full release gate) all green with postgres running; biome clean; DEBUG sweep empty. ADRs left frozen as decision records (closure tracked in the coverage matrix and this changelog, per convention). With BH archived the active SaaS-Readiness scope is Epic BI alone (SDK Stabilization & Publication, 21 story points).
- v0.37.0 - Closed **Epic BG — Backend-Authoritative Lease Clock + Side-Effect-Once (KRT)**, the third epic of the SaaS-Readiness block, realizing ADR-050 (the execution-lease clock is backend-authoritative for shared multi-worker backends; single-writer embedded backends keep the in-process clock) and ADR-052 (side-effect-once under preemption via a `(runId, callId, fencingToken)` idempotency envelope, no-retry of in-flight `nonRetryable` work on authority loss, and client-result-as-proposal) for PRD CAP-P0-068 exactly as accepted — no ADR text changed; it refines the already-sound lease/fencing model rather than rebuilding it. All five tickets landed and each milestone was independently reviewed by an isolated `reviewing-milestone-commits` subagent over its commit SHA, with valid P0/P1/structural findings fixed (via a review-follow-up commit) before the next milestone. **KRT-BG001** aligned machine authority first (declare-then-promote, the BE007/BF007 precedent): classified the kernel §5.2 backend-authoritative lease-clock note and the framework "Running Lease Ownership" side-effect-once / client-result-as-proposal notes in the docs-to-authority coverage matrix as `missing-conformance-follow-up` tracked to the BG tickets, and registered the run-liveness authority-packet + conformance-plan references — no runtime behavior change. **KRT-BG002** made `@tuvren/backend-postgres` stamp and evaluate lease expiry using PostgreSQL server time captured once per transaction (`clock_timestamp()`, surfaced as `RuntimeBackendTx.now()`), added the `shared-lease-clock` `BackendCapability` bit (Postgres `true`; memory and single-file SQLite `false`, keeping the in-process clock), re-based worker-supplied absolute lease expiry onto backend time, and measured the framework renewal margin by elapsed local monotonic delta for shared-clock deployments so backend-time expiry is never mixed with worker-time now. **KRT-BG003** threaded the idempotency identity derived from `(runId, callId, fencingToken)` onto the Tuvren-server tool dispatch path (exposed via `ToolExecutionContext`) and the Client Endpoint Boundary `ClientInvocationEnvelope`, reusing the existing `idempotent`/`nonRetryable`/`maxRetries` metadata with no new tool-definition fields. **KRT-BG004** stopped retrying an in-flight `nonRetryable` invocation once execution authority is lost (the abort surfaced via `environment.signal.aborted`; durably-staged completed results are still recovered by `callId` per §4.9), added a `reason: "run_authority_lost"` discriminator distinguishing preemption from the per-dispatch token-mismatch guard, and gated client-reported results behind a valid run fencing token so a stale or late client report cannot mutate committed history; the review follow-up added a client-path non-mutation integration test driving a real client-endpoint dispatch into an aborted context. **KRT-BG005** promoted the declared surface to runnable, registered, evidence-backed conformance: a `kernel.shared-lease-clock`-gated `clock_skew_preemption` check on `kernel-run-liveness.json` (plan 0.1.0 → 0.2.0; kernel-protocol packet 0.9.0 → 0.10.0) drives two owners against one PostgreSQL backend under simulated worker-clock skew and proves no split-brain preemption while the backend clock judges the lease live, genuine preemption once the backend clock advances past expiry, the completed side-effecting call incorporated by `callId` (driven at most once) with no uncommitted residue; and a `client-result-as-proposal-authority-lost` check on `runtime-api-lifecycle-extended.json` (plan 0.1.0 → 0.2.0; shared-core packet 1.6.0 → 1.7.0) proves a client result returning after authority loss is never committed. Both run-liveness matrix claims flipped to `authority-backed-conformance-covered`; the kernel adapters advertise `kernel.shared-lease-clock` (PostgreSQL only) while staying assertion-free, and per-backend compatibility evidence was refreshed. The BG005 independent review returned no P0/P1 (one P2 wording nit on the `recoveryHeadMatchesBranchHead` assertion comment, fixed at closure). **Realization-accuracy note recorded in `tech-spec/changelog.md` v0.30.3 (ADRs left frozen, BE/BF precedent):** the `(runId, callId, fencingToken)` triple is a per-attempt envelope key, not a recovery-stable identity (`runId` is fresh per run; `fencingToken` rotates on renewal/recovery; only `callId` is stable across recovery per §4.9), so ADR-052 decision 1's "re-dispatch after recovery presents the same identity" holds within a run/fencing epoch for external dedup, while cross-recovery side-effect-once is achieved by the composition of ADR-050 backend-clock no-split-brain + no-retry + recovery-skip-by-`callId` + client-result-as-proposal — which is what the conformance proves. Close: archived the epic via `git mv` into `.constitution/tasks/completed/`; recomputed active story points 60 → **37 remaining** (BH 16, BI 21) and marked Epic BG CLOSED across the executive summary, critical path, active scope, build-order flowchart, and definition of done; recorded the realization in `tech-spec/changelog.md` v0.30.3 and added Epic BG to the completed-work ledger. Validation: `bun run check`, `bun run verify:kernel` (memory/SQLite/PostgreSQL conformance incl. the new clock-skew check), and `bun run verify` (full release gate) all green; biome clean; DEBUG sweep empty. ADRs left frozen as decision records (closure tracked in the coverage matrix and this changelog, per convention).
- v0.36.0 - Closed **Epic BF — Data Lifecycle: Reclamation + Crypto-Shredding Erasure (KRT)**, the data-lifecycle epic of the SaaS-Readiness block, realizing ADR-051 (a kernel reachability-reclamation primitive plus crypto-shredding erasure of host-key-encrypted untrusted-edge payloads) for PRD CAP-P0-066 (reclamation mechanism) and CAP-P0-067 (right-to-erasure) exactly as accepted — no ADR text changed. All seven tickets landed and each milestone was independently reviewed by an isolated `reviewing-milestone-commits` subagent over its commit SHA, with blocking findings fixed before the next milestone. **KRT-BF001** aligned machine authority first (declare-then-promote, the BE007 precedent): added the `maintenance.reclamation` `BackendCapability` bit, declared `bindingSections.reclamation` plus the `kernel-reclamation.json` conformance-plan stub in the kernel-protocol authority packet (unregistered, because the portability and compatibility-evidence gates forbid a runnable plan before an adapter advertises the capability), and re-pointed the §9.4 freeze-gate classification to `KRT-BF007` — no runtime behavior. **KRT-BF002** recorded the crypto-shredding design in `.constitution/spikes/SPK-BF002.md`: an opaque ciphertext envelope (`{ keyRef, ciphertext, alg, iv, tag }`, `TVE1` magic prefix), an interface-first `PayloadCodec` contract with a default AES-256-GCM codec consuming host-supplied key bytes from a host keyring, per-scope keyRef granularity (a scope-wide key destruction shreds that scope's untrusted-edge payloads), the encrypt-on-write/decrypt-on-read edge seam, and a typed erased-read result; keys stay host-held (ADR-001 + Secret Isolation Model preserved). **KRT-BF003** implemented the reachability mark-and-sweep in the kernel over the memory backend: a capability-gated `maintenance.reclaim` surface where keep-closure = reachability from live roots (non-archived branch heads, thread roots, active-run staged work) unioned with everything created at or after the grace horizon (the oldest active run's `createdAtMs`, else unbounded), sweeping only records outside the keep set and older than the horizon, with structural sharing honored because a shared Object stays marked via any live root. **KRT-BF004** extended reclamation to SQLite (scoped `DELETE`s) and PostgreSQL (snapshot-draft mutation + re-persist) and advertised `maintenance.reclamation: true` on all three production backends, with a non-supporting backend rejecting reclamation via `kernel_capability_unsupported`. **KRT-BF005** shipped the crypto-shredding envelope: `@tuvren/core/lifecycle` exports the envelope + `ErasedPayload` types, the `PayloadCodec` interface, and `createAesGcmPayloadCodec({ keyring })`, re-exported from `@tuvren/runtime`; the framework encrypts on write and decrypts on read at the provider/tool/MCP/client edges, opt-in via `createTuvren({ payloadCodec })` (default plaintext, back-compatible); destroying the host key renders payloads unrecoverable while the lineage hash structure stays byte-identical, and erased reads surface a typed total result rather than crashing. **KRT-BF006** exposed the host-facing maintenance surface on `TuvrenRuntime` and the §4.17 tenant-offboarding flow (destroy the scope's keys + reclaim + drop the scope partition per backend), proving offboarding scope A leaves no other scope affected (cross-scope isolation preserved from BE). **KRT-BF007** promoted the declared surface to runnable, registered, evidence-backed conformance: the kernel conformance adapters advertise `kernel.reclamation` and answer reclaim-probe and erasure-probe operations (the adapter plays the §4.17 host role, owning the codec and key so the kernel stays codec-unaware and stores only opaque envelopes), the plan is registered in the authority packet, and the §9.4 reclamation claim flips to `authority-backed-conformance-covered` with reclaim-probe + erasure-probe evidence across memory/SQLite/PostgreSQL (all five checks pass); it also closed four carried-forward review follow-ups (structurally-shared-object retention, cross-grace-horizon reference closure, a `validateCommittedState` observe-annotation referential invariant, and an encrypted context-engineering rewrite-path test). The M7 review triage applied one freeze-scope fix (`bcf7282a`): the §9.4 flip had over-promoted the non-support `kernel_capability_unsupported` rejection row, which is now left to the canonical capability-gating classifier (§9 → `KRT-AM010`, ADR-034) since the promoted plan only exercises capability-bearing backends. Close: archived the epic via `git mv` into `.constitution/tasks/completed/`; recomputed active story points 99 → **60 remaining** (BG 23, BH 16, BI 21) and marked Epic BF CLOSED across the executive summary, critical path, active scope, build-order flowchart, and definition of done; recorded the realization in `tech-spec/changelog.md` v0.30.2 and added Epic BF to the completed-work ledger. ADRs left frozen as decision records (closure tracked in the §5.x posture sections and the coverage matrix, per convention).
- v0.35.0 - Closed **Epic BE — Tenancy Scope Seam + Isolation-by-Construction (KRT)**, the keystone of the SaaS-Readiness block, realizing ADR-048 (scope seam bound at backend/connection construction; kernel syscall surface stays scope-free) and ADR-049 (isolation-by-construction; durable identity is scope-resolved with no cross-scope dedup) for PRD CAP-P0-064/065. All eight tickets landed and each milestone was independently reviewed by an isolated `reviewing-milestone-commits` subagent over its commit SHA with blocking findings fixed before the next milestone. **KRT-BE001** aligned machine authority first so the docs-to-authority freeze gate accepts the new surface: classified the kernel `docs/KrakenKernelSpecification.md` §2.3 scope-resolved-identity claims (and the framework scope notes) in the coverage matrix, declared the `bindingSections.scope-isolation` surface plus the `kernel-scope-isolation.json` conformance-plan reference in the kernel-protocol authority packet, and wired the freeze-gate classifier — no runtime behavior change. **KRT-BE002** recorded the per-backend realization decision in `.constitution/spikes/SPK-BE002.md` (memory scope-keyed stores; SQLite file-per-scope; PostgreSQL scope-keyed snapshot row under row-level isolation), correcting the initial Postgres sketch to a `(snapshot_id, scope)` composite-key row rather than a dedicated schema/connection. **KRT-BE003** extended the backend-construction contract so a host binds a Scope at construction with no kernel syscall change and realized it in `@tuvren/backend-memory` (scope-keyed `MemoryScopeStore`; `createMemoryBackend({ scope, store })`; identical content under scope A and scope B is independent and non-cross-observable; `createTuvren`/`createRuntimeKernel` thread the scoped backend unchanged; `listThreads` needs no scope parameter). **KRT-BE004** realized SQLite scope binding as file-per-scope: the default Scope maps to `databasePath` verbatim (existing single-scope databases keep working unchanged) while any other Scope derives a deterministic sibling file, so two scopes hold independent durable objects and no query crosses a scope. **KRT-BE005** realized PostgreSQL scope binding via row-level isolation — a scope-keyed snapshot row with composite primary key `(snapshot_id, scope)` so backends sharing one schema never observe each other's rows and there is no cross-scope dedup — and serialized concurrent first-touch schema initialization with a per-schema transaction-scoped advisory lock (hosts reconstructing per-request scoped backends routinely race on first touch). **KRT-BE006** proved durable identity resolution and the full Durable-Read Surface (`listThreads`, `listBranches`, state-at-TurnNode, history walk, branch messages, `store.has`/`store.get`) scope-confined across all three backends — composed-surface proof at the framework layer (memory, bun-native) plus per-backend substrate-confinement proofs in each native runner — with `store.has(hash)` returning false in scope A for content only present in scope B. **KRT-BE007** promoted the cross-scope isolation conformance to runnable, evidence-backed coverage: the `kernel-scope-isolation.json` plan with a non-grading adapter cross-scope probe (raw enumeration / `store.get` / `store.has` observations), registered in the kernel-protocol authority packet (bumped 0.5.0 → 0.6.0) across `conformancePlans`/`authoritativeSources`/`verificationPaths`, the §2.3 freeze claim promoted to authority-backed conformance, with per-backend evidence recorded; the enumeration check is gated on the `thread.enumeration` capability so non-enumerating backends record it non-applicable rather than failing. **KRT-BE008** made operational telemetry and transcripts carry the Scope as correlation context and never emit another scope's data: added `tuvren.runtime.scope.id` to the Weaver semconv (regenerating the TypeScript + Rust constants, `otel-attributes.json`, and the semantic-conventions reference), a required `scope` on `TelemetryLineage` stamped on every event and span, the OTel sink mapping, and `RuntimeCoreOptions.scope` (defaults to `DEFAULT_SCOPE`, rejects an empty binding) which is correlation-only and never crosses the kernel seam; the REPL host accepts `--scope`/`TUVREN_REPL_SCOPE` and binds it to both the durable backend and the runtime telemetry, records the resolved Scope in the transcript header (`config.scope`, optional for backward-compatible reading) and re-binds it on replay; and the `scope-isolation.telemetry-and-transcript-correlation` check (operational-telemetry plan bumped 0.2.0 → 0.3.0, shared-core packet pin matched) constructs two runtimes bound to distinct Scopes and asserts each scope's telemetry/transcript surfaces carry only that Scope (`resultField`) and none of the other's identifier (`secretAbsence`). Validation: framework conformance 462/462 applicable checks pass (474 total, 12 non-applicable); the compatibility matrix and evidence were refreshed with postgres running; `bun run codegen`, the docs-to-authority freeze gate (264 classified claims), targeted typechecks, the host-repl (127) / framework-runtime (504) suites, and a Rust kernel `cargo check` all pass. The keystone epic gating BF (data lifecycle) and BI (SDK freeze) is closed; with BE archived the active SaaS-Readiness scope is Epics BF–BI (99 story points).
- v0.34.0 - Opened the **SaaS-Readiness block (Epics BE–BI)** as the active execution plan, recasting the previously named-but-unticketed productionization roadmap (former Epics BE–BI) into 30 atomic `KRT` tickets across five epics (133 active story points: BE 34, BF 39, BG 23, BH 16, BI 21), tracing to PRD v0.10.0 (CAP-P0-064..070), Architecture v0.10.0, and TechSpec v0.30.0 (ADR-048..055). Created `active/EPIC-BE-tenancy-scope-seam-isolation.md` (scope bound at backend construction across memory/SQLite/PostgreSQL; isolation-by-construction; durable-read scope safety; scope-tagged telemetry; cross-scope conformance), `active/EPIC-BF-data-lifecycle-reclamation-erasure.md` (kernel `maintenance.reclamation` primitive; host-key-encrypted crypto-shredding envelope at the provider/tool/MCP/client edges; tenant-offboarding flow; data-lifecycle conformance), `active/EPIC-BG-lease-clock-side-effect-once.md` (PostgreSQL backend-time lease clock + capability bit; `(runId,callId,fencingToken)` idempotency envelope; no-retry of in-flight `nonRetryable` on lease loss; client-result-as-proposal; clock-skew conformance), `active/EPIC-BH-conversation-state-ownership.md` (reconstruct-from-DAG; shreddable continuity artifacts; AY005 multi-turn round-trip close; correctness-neutral caching; AI SDK bridge `providerExecuted`/`dynamic` fidelity audit), and `active/EPIC-BI-sdk-stabilization-publication.md` (stable-core API audit; experimental `@tuvren/core/capabilities` marking; semver freeze + API-stability gate; registry publication; adopter onboarding — gated after BE+BF per ADR-054). Two spikes seeded: `.constitution/spikes/SPK-BE002.md` (scope-binding realization per backend) and `SPK-BF002.md` (crypto-shredding envelope + host-key custody). Rewrote `critical-path.md`: new active story-point total and full critical path, Block-6 active scope, the active-only build-order Mermaid graph, the SaaS-Readiness Definition of Done, the ~3k–8k LoC epic-sizing heuristic, publication moved from deferred to active (Epic BI), and the residual adoption themes recast as deferred Epics BJ–BM (native provider clients behind the ADR-055 trigger, performance characterization, documentation/onboarding, reference application). Planning-branch note recorded: the kernel v0.12 / framework v0.21 spec sections are target authority classified honestly in the docs-to-authority freeze gate (version-status headers as `explicitly-deferred`; each target claim as `missing-conformance-follow-up` tracked to its owning epic's first ticket — `KRT-BE001`/`BF001`/`BG001`/`BH001`), so `bun run verify` passes green on this branch with no false conformance coverage asserted; each epic's first ticket then promotes its tracked claim to `authority-backed-conformance-covered` as the implementation lands. No implementation code, schema, or generated artifact was changed in this planning pass.
- Migrated the monolithic `constitution/Tasks.md` into the modular `.constitution/tasks/` directory: active epics under `active/` (EPIC-BC, EPIC-BD), closed epics under `completed/`, and the summary/build-order/DoD in `critical-path.md`. Content preserved verbatim; ticket IDs and Gherkin unchanged.
- v0.33.0 - Closed Epic BD Trust-Boundary Security Hardening — the final active epic; with its closure the active execution plan is empty (0 remaining story points). Realized ADR-043 (framework-enforced execution bounds) and ADR-044 (secret isolation) and independently verified the approval and untrusted-input trust boundaries (ADR-039/ADR-044). KRT-BD005: added `ExecutionBounds`/`ExecutionBoundExceededDetails` to `@tuvren/core/execution`, the `execution_bound_exceeded` error code, and the cooperative `TuvrenPrompt.signal` field on the provider-api authority, with packet bumps. KRT-BD006 (the heaviest ticket): implemented the framework bounds guard in `@tuvren/runtime` enforcing `maxIterations`/`maxToolCalls`/`maxWallClockMs` above driver discretion, clamping `AgentConfig.maxIterations`/`maxParallelToolCalls`/`defaultMaxParallelToolCalls`, throttling tool concurrency to `maxConcurrentToolCalls`, propagating the wall-clock-deadline abort through `TuvrenPrompt.signal`/`ToolExecutionContext.signal` (forwarded by the owned `bridge-ai-sdk` and tool paths), ignoring late completions, finalizing a breach as a `failed` `ExecutionResult` with a fatal canonical `error` event before the failed `turn.end`, wiring the terminal `ignored` `InvocationLifecycleState`, emitting the `execution.bounded` telemetry event (semconv + regenerated allowlist updated together), rejecting invalid bounds at construction, and rejecting double-supplied bounds as `invalid_createtuvren_options`. KRT-BD007: added the `runtime-api-execution-bounds` check set using a runaway aimock driver that always requests continuation, covering every Gherkin clause. KRT-BD002: added the transcript backend-options redactor to `@tuvren/repl-host` masking PostgreSQL `connectionString`/`password` and credential-shaped backend options (libpq + cloud aliases) while keeping replay topology, with TDD coverage. KRT-BD003: added edge-confinement READMEs and reusable secret-isolation fixtures. KRT-BD004: added the `secret-isolation` check set across `providers-mcp-client.json`, `framework-operational-telemetry.json`, and `runtime-api-callables-extended.json`, backed by a new shared runner-owned secret-absence helper and `secretAbsence` assertion kind covering raw, bearer-prefixed, header-normalized, URL-encoded, base64/base64url, and partial-token leak forms over persisted records, stream events, telemetry, and an in-process recorded transcript, with a helper unit test proving each variant. KRT-BD009: added the independent `trust-boundary` check set to the runtime-api and MCP plans pinning non-bypassable approval gating, `tool_input_validation_failed` for local input violations, and `mcp_tool_input_invalid` for MCP-advertised input violations rejected before transport — no implementation gap was exposed. KRT-BD008: added the normative §4.12 Execution Bounds section to `docs/KrakenFrameworkSpecification.md` (bumped to v0.20), extended the docs-to-authority freeze gate (253 classified claims), and ran the full clean `bun run verify` (exit 0 across all gates with postgres) plus refreshed compatibility evidence reflecting the execution-bounds, secret-isolation, and trust-boundary lanes. Each milestone was independently reviewed by an isolated `reviewing-milestone-commits` subagent over its commit SHA with P0/P1 findings fixed before continuing (the M8 review surfaced and fixed a one-token spec/authority drift where §4.12 listed `maxConcurrentToolCalls` in the breach-details `bound` union, which the `ExecutionBoundKind` authority intentionally excludes). The Tooling block (Epics AW–BC) and the trust block (Epic BD) are both closed; the productionization roadmap (Epics BE–BI) remains named but not yet ticketed.
- v0.32.0 - Closed Epic BC Tooling Restructuring Closeout: cross-class integration conformance check set (KRT-BC001) added 19 new `bc.bc001.*` checks exercising all four execution classes in one agent segment, MCP-as-binding endpoint classification, exposure-time and invocation-time policy co-active, and per-class observation limits — bringing framework conformance to 446/446 applicable checks (458 total, 12 non-applicable) all passing; `docs/KrakenFrameworkSpecification.md` bumped to v0.19 with a normative §11 Capability Orchestration section (KRT-BC002) covering the Tool Surface vs Capability distinction, the four execution classes, bindings and endpoint kinds, exposure-time and invocation-time policy decision points, per-class observation limits, and the conceptual invariant; portability inventory (`epic-al-portability-inventory.json`) bumped to v0.4.0 with the capability-orchestration-integration plan registered as a required authoritative source for `tuvren.shared.core` (KRT-BC003); docs-to-authority freeze gate extended with `capabilityOrchestration` evidence template and §11 classifier — all 246 classified framework normative claims pass; compatibility evidence refreshed with postgres running (KRT-BC004); `bun run verify` exits 0 across all 27 gates. The Tooling block (Epics AW–BC) is fully closed.
- v0.31.6 - Closed Epic BB Exposure & Invocation Policy Model: `PolicyCapabilityMetadata` added to `@tuvren/core/capabilities` carrying per-capability policy dimensions (riskClass, requiredResidency, requiresUserPresence, requiredCredentialScopes, nonRetryable); `CapabilityPolicyContext` extended with BB001–BB004 dimensions (allowedResidencies, userPresent, unavailableCapabilityIds, availableCredentialScopes, capabilityMetadata); `TuvrenToolDefinition` gains five optional policy fields; `CapabilityPolicyContextInputs` new interface and `AgentConfig.policyContextInputs` added; full five-dimension policy engine (residency, risk/approval, active-endpoint, user-presence, credential-boundary, nonRetryable); exposure-time filtering wired in `createDriverExecutionContext` (withheld tools never reach the model); invocation-time context populated from real `AgentConfig.policyContextInputs`; resume-path invocation check added to `resolveResumeDecision` (and `createToolBatchEnvironment` in tool-resume path fixed to carry policy engine); `nonRetryable: true` overrides `idempotent: true` in retry budget; risk-based `requiresApproval` gate bridges policy decision into pending-approval flow; composition: deny-from-any-dimension with composed non-secret reason; deterministic; `capability-policy` conformance check set (26 checks covering BB001–BB005 at both decision points + composition + control path) registered in the `tuvren.shared.core` authority packet; compatibility evidence refreshed. 472 runtime tests pass; 425/425 framework conformance checks pass; `bun run verify` exits 0 (aside from worktree-only generated-artifact issues resolved by `bun run codegen`).
- v0.31.5 - Closed Epic BA Invocation Lifecycle & Observation Model: added `InvocationLifecycleState` union type to `@tuvren/core/capabilities` formalising the uniform cross-class lifecycle (resolved → policy-admitted → dispatched → completed/failed/ignored) per KRT-BA001; routed provider-native and provider-mediated `tool.start`/`tool.result` attribution events through `publishRuntimeEvent` so the telemetry emitter observes them alongside the canonical stream, closing the BA002 telemetry-consistency gap; `null` used as the JSON-serializable "not observed" sentinel for provider tool inputs (passes `assertTuvrenStreamEvent` validation); BA003 cross-class resume and recovery semantics proven: tuvren-server fails clean via existing durability rules, provider-native/mediated resolve from observed driver output, tuvren-client stale/unavailable paths surface typed error codes (CAPABILITY_RESULT_STALE, CAPABILITY_BINDING_UNAVAILABLE), and turn abort terminates as `execution_cancelled` without fabricated results; BA004 lifecycle telemetry depth confirmed: `tool_call` spans keyed to runtime lineage and execution class for all four classes using existing semconv attributes (no extension needed); `invocation-lifecycle-observation` conformance check set (19 checks) added to the authority packet and registered as an executable verification path with full BA001–BA003 coverage including fail-clean proofs; 424 runtime tests pass; 399/399 framework conformance checks pass; `bun run verify` exits 0.
- v0.31.4 - Closed Epic AZ Tuvren-Client Execution Class: implemented the runtime-side leased client-endpoint protocol and attachment seam (concrete endpoints remain host-developer deliverables); `AttachedClientEndpoint`, `ClientEndpointCapabilityAdvertisement`, `ClientInvocationEnvelope`, `ClientReportedResult`, `ClientEndpointBoundary`, `ClientDispatchResult` shapes added to `@tuvren/core/capabilities`; `AgentConfig.clientEndpoints` and `AgentConfig.clientEndpointBoundary` wired; `ClientEndpointBoundary.detach()` supports dynamic endpoint lifecycle; synthetic `TuvrenToolDefinition` entries built from advertised capabilities route dispatch through the boundary with leaseToken staleness detection; `isClientEndpointTool` guard suppresses `tool.audit` events and server-side rate-limiting for the class (canAudit: false); `observationForClass("tuvren-client")` explicit with partial-observability limits; client-side MCP classified as `tuvren-client / mcp-server` endpoint kind (never reclassified); `PauseContext` and `LoopState` carry the boundary through pause/resume and handoff cycles; `tuvren-client-execution-class` conformance check set (13 checks) added to the authority packet and registered as an executable verification path; `createClientEndpointBoundary` exported from `@tuvren/runtime`; integration contract documented at `boundaries/framework/contracts/client-endpoint-integration.md`; 394 runtime tests pass; 379/379 framework conformance checks pass; kernel verify:kernel:fresh passes.
- v0.31.3 - Closed Epic AY Provider-Native & Provider-Mediated Execution Classes: added `ProviderNativeToolDeclaration` and `ProviderMediatedToolConfig` to `TuvrenPrompt`/`AgentConfig`; wired the AI SDK bridge to accept declared provider tool results (`LanguageModelV3ToolResult`) via `providerToolClassLookup`; threaded `providerNativeTools`/`providerMediatedTools`/`providerContinuity` through `createProviderPrompt`/`createAroundModelContextSnapshot`; pre-staged provider tool messages bypass the Tool Execution Gateway; `emitProviderToolAttributionEvents` emits `tool.start`+`tool.result` with `owner:"provider"` and correct per-class observation limits (canAudit/canCancel/canRetry/canResume: false); `isProviderOnlyResponseEventSet` guard in `validateDriverAssistantEvents` handles pure provider-stream responses; `assertDriverMessages` guard extended to allow pre-staged provider tool messages; concrete proofs through the full stack (bridge → react-driver → runtime) for Anthropic `code_execution_20260120` pattern (generate path) and OpenAI `openai.mcp` pattern; `provider-native-execution-class` (10 checks) and `provider-mediated-execution-class` (10 checks) conformance check sets added to the `tuvren.providers.provider-api` authority packet; 54 bridge tests + 358 runtime tests + 78 react-driver tests pass; 52/52 provider conformance checks pass. Known gap: AY005 multi-turn providerContinuity round-trip (extraction from response → next prompt) is structurally wired but not exercised by a multi-turn test; single-turn proofs validate all other invariants.
- v0.31.2 - Closed Epic AX Tuvren-Server Execution Class: implemented full server lifecycle (input/output validation with `tool_input_validation_failed`/`tool_result_validation_failed` error codes, `TuvrenToolDefinition.outputSchema`), idempotent retry (`idempotent`, `maxRetries` fields, framework-owned retry loop, cooperative cancellation, late-completion ignoring), tenant isolation and rate-limiting (`AgentConfig.serverExecution`, `ServerRateLimiter`, `TOOL_INVOCATION_RATE_LIMITED` per-turn per-instance), server-side MCP binding classification confirmed (`mcp-server` endpoint kind), server sandbox endpoint (`TuvrenSandboxExecutor`, `metadata.sandbox.endpointId`, `tuvren-sandbox` endpoint kind, `AgentConfig.sandboxExecutors`), full-lifecycle `ToolAuditEvent` (`tool.audit`) at input/output validation, retry, and rate-limit lifecycle points with secret isolation, and the `tuvren-server-execution-class` conformance check set (19 checks including cancellation/late-completion, tenant isolation, and output-validated audit). 349 runtime tests pass; 19/19 AX conformance checks pass; 37 pre-existing non-AX conformance failures unchanged.
- v0.31.1 - Closed Epic AW Capability Orchestration Foundation: added `@tuvren/core/capabilities` subpath (§3.13 types), Capability Registry, Binding & Endpoint Resolver (back-compat `defineTool` → `tuvren-server`), Capability Policy Engine (exposure-time and invocation-time decision points, wired into tool dispatch), execution-class + owner attribution on canonical events and telemetry (semconv extended), `capability_binding_unavailable` error code, and the `runtime-api-capability-orchestration` foundation conformance check set (12 new checks including `tool.result isError` wired denial proof). Authority packet bumped to v1.2.0. 303 runtime tests, 347/347 framework conformance checks pass.
- v0.31.0 - Restructured the capability/tool restructuring into a contiguous, fully-ticketed **Tooling block (Epics AW–BC)** placed at the front of the queue, implementing the PRD v0.9.0 / Architecture v0.9.0 / TechSpec v0.29.0 capability-orchestration model (ADR-046, ADR-047): AW Capability Orchestration Foundation, AX Tuvren-Server Execution Class, AY Provider-Native & Provider-Mediated Execution Classes, AZ Tuvren-Client Execution Class, BA Invocation Lifecycle & Observation Model, BB Exposure & Invocation Policy Model, BC Tooling Restructuring Closeout. Tuvren-client scope is runtime protocol + attachment seam only (concrete client endpoints stay host deliverables); provider-native/mediated scope is runtime support proven against the AI-SDK-bridged providers. The whole block precedes the trust block and the productionization roadmap, so the former Epic AW (Trust-Boundary Security Hardening) is renumbered to **Epic BD** and the roadmap shifts to **Epics BE–BI**.
- v0.29.2 - Closed Epic AV operational telemetry: added `@tuvren/core/telemetry`, framework sink wiring with secret-screening, `@tuvren/telemetry-otel`, the `framework-operational-telemetry` conformance plan, curated runtime re-exports, portability-inventory updates, and verification coverage.
- v0.29.1 - Maintenance alignment after Epic AU closure: compacted completed Epics AM-AU into the closed-work ledger, marked AU closed with `kernel-crash-recovery` evidence, and updated the active graph/DoD accordingly.
- ... [Older history truncated, refer to git logs]
