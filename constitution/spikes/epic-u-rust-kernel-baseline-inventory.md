# Epic U Rust Kernel Baseline Inventory

## Status

Epic U is closed in current repo reality.

## Delivered Scope

- Added the root Cargo workspace with Rust `1.95.0`, edition `2024`, and kernel-owned Rust members under `boundaries/kernel/implementations/rust/`.
- Added Devenv Rust support through `languages.rust.toolchainFile = ./rust-toolchain.toml`, plus the required `rust-overlay` input and `protobuf` package for Cargo-time Tonic/Prost generation.
- Implemented `tuvren-kernel-rust` with protocol/domain types, deterministic SHA-256 identity helpers, explicit deterministic CBOR value encoding, and a process-local in-memory kernel surface.
- Implemented Rust conformance execution over the boundary-owned kernel fixture suite `tuvren.kernel.protocol-seed@0.1.0`.
- Implemented `tuvren-kernel-rust-grpc-service` for the governed `tuvren.kernel.interop.v1` proto surface, including unary kernel operations, `NodeWalkBack` streaming, stable kernel error-code to gRPC status mapping, and a runnable Tonic server entrypoint.
- Enabled generated Rust telemetry helper output from `telemetry/semconv/tuvren-runtime.yaml` under the Rust kernel consumer tree.
- Added Nx targets for Rust build, test, lint, conformance, and interop smoke lanes, and added Rust lanes to repo verification.

## Scope Boundaries

- No host-facing TypeScript runtime API changed.
- No TypeScript transport client, runtime switch, or TypeScript framework to Rust kernel path was added; that remains Epic V scope.
- No Rust SQLite/storage backend was added; Epic U uses the process-local in-memory kernel baseline only.
- Rust gRPC bindings are generated by Cargo `build.rs` from the existing proto authority and are not checked in.
- The Rust gRPC smoke validates a local TCP Tonic server/client path. TypeScript-to-Rust transport clients and runtime selection remain Epic V scope.

## Evidence

- `devenv shell -- rustc --version`
- `devenv shell -- cargo fmt --all -- --check`
- `devenv shell -- cargo clippy --workspace --all-targets -- -D warnings`
- `devenv shell -- cargo test --workspace`
- `bun run nx run kernel-rust-conformance-runner:conformance`
- `bun run nx run kernel-rust-grpc-service:interop-smoke`
- `bun run nx run kernel-interop-grpc:lint`
- `bun run nx run kernel-interop-grpc:breaking`
- `bun run typecheck`
- `bun tools/scripts/verify.ts`

## Handoff To Epic V

Epic V can now start from a real Rust kernel transport baseline. The remaining work is TypeScript-side transport client/runtime selection, measured TS-framework-to-Rust scenarios, compatibility-ledger updates from real evidence, and cross-language telemetry validation.
